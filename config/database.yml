default: &default
      adapter: postgresql
      encoding: unicode
      pool: 5
      username: username
      password: password
      host:     <%= ENV['IP'] %>

development:
  <<: *default
  database: expensetracker_development

test:
  <<: *default
  database: expensetracker_test

production:
  <<: *default
  database: expensetracker_production